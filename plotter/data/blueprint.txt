with p.Canvas("", (1, 3), save="", show=False) as canvas:
    canvas.setup(0)
    canvas.setup(1)
    canvas.setup(2)

    p.ScatterPlot(x, y, y_err, x_err).draw(canvas)
    p.LinePlot(x, f, wider=(0.01, 0.01)).draw(canvas)
    p.BarChart(x_bars, heights).draw(canvas, 1)

    hist = p.Hist(data, density=True)
    hist.draw(canvas, 1)
    p.LinePlot(hist.bins, fit).draw(canvas, 1)

    hist2d = p.Hist2D(a, b, 100)
    hist2d.draw(canvas, 2)
